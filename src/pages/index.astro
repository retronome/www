---
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
---

<!doctype html>
<html lang="en">
    <head>
        <title>{SITE_TITLE}</title>
        <title>Astro Blog</title>
        <!-- Global Metadata -->
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width,initial-scale=1" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="sitemap" href="/sitemap-index.xml" />
        <link
            rel="alternate"
            type="application/rss+xml"
            title="Astro Blog"
            href="https://example.com/rss.xml"
        />
        <meta name="generator" content="Astro v5.8.0" />
        <!-- Font preloads -->
        <link
            rel="preload"
            href="/fonts/atkinson-regular.woff"
            as="font"
            type="font/woff"
            crossorigin
        />
        <link
            rel="preload"
            href="/fonts/atkinson-bold.woff"
            as="font"
            type="font/woff"
            crossorigin
        />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Anton&family=Bungee+Outline&family=Bungee+Shade&display=swap"
            rel="stylesheet"
        />

        <!-- Canonical URL -->
        <link rel="canonical" href="https://example.com/retronome" />
        <!-- Primary Meta Tags -->
        <title>{SITE_TITLE}</title>
        <meta name="title" content="{SITE_TITLE}" />
        <meta name="description" content="{SITE_DESCRIPTION}" />
        <!-- Open Graph / Facebook -->
        <meta property="og:type" content="website" />
        <meta property="og:url" content="http://localhost:4321/retronome" />
        <meta property="og:title" content="Astro Blog" />
        <meta property="og:description" content="{SITE_DESCRIPTION}" />
        <meta
            property="og:image"
            content="http://localhost:4321/blog-placeholder-1.jpg"
        />
        <!-- Twitter -->
        <meta property="twitter:card" content="summary_large_image" />
        <meta
            property="twitter:url"
            content="http://localhost:4321/retronome"
        />
        <meta property="twitter:title" content="Astro Blog" />
        <meta property="twitter:description" content="Welcome to my website!" />
        <meta
            property="twitter:image"
            content="http://localhost:4321/blog-placeholder-1.jpg"
        />
        <style>
            @keyframes typing {
                from {
                    width: 0;
                }
                to {
                    width: 100%;
                }
            }

            @keyframes blink {
                to,
                from {
                    /* Blink during typing (first 1.5s out of 2.25s total) */
                    border-color: black;
                }
                100% {
                    border-color: transparent;
                }
            }

            html {
                height: 100%;

                * {
                    margin: 0px;
                    padding: 0px;
                }
            }

            body {
                background-color: #fff4ec;
                height: 100%;
            }

            main {
                position: relative;
                display: flex;
                width: 920px;
                /* max-width: calc(100% - 2em); */
                gap: 0;
                margin: auto;
                height: 100%;
                /* background: cyan; */

                &:has(label:hover) > label span {
                    transform: rotate(90deg) translateY(0);
                }

                > label {
                    color: white;
                    cursor: pointer;
                    font-family: "Anton", sans-serif;

                    > div {
                        width: 46px;
                        height: 100%;
                        padding-top: 230px;
                        overflow: hidden;

                        > span {
                            display: inline-block;
                            width: 100%;
                            transform: rotate(90deg) translateY(-200%);
                            transition: transform 0.3s ease;
                        }
                    }

                    > div.red {
                        background: #e65d43;
                    }

                    > div.orange {
                        background: #e59336;
                    }

                    > div.gold {
                        background: #e7b242;
                    }

                    > div.teal {
                        background: #008989;
                    }
                }

                & input[type="radio"] {
                    display: none;

                    + div {
                        max-width: 6px;
                        overflow: hidden;
                        transition: max-width 0.5s;

                        > section {
                            width: 680px;
                            padding: 200px 20px 0px 20px;

                            > h1 {
                                font-family: "Bungee Shade", sans-serif;
                                font-size: 4em;
                            }

                            &:last-of-type h1 {
                                overflow: hidden;
                                border-right: 2px solid;

                                animation:
                                    typing 1.5s steps(10, end) forwards,
                                    blink 1.5s step-end forwards;

                                transition: border-right 1.5s 0;
                            }
                        }
                    }

                    &:checked + div {
                        max-width: 100%;
                    }
                }
            }

            /* Contact Section */
            /* .contact-form {
                max-width: 500px;
                margin: 2rem 0;
            }

            .form-group {
                margin-bottom: 1.5rem;
            }

            .form-group label {
                display: block;
                margin-bottom: 0.5rem;
                font-family: "Anton", sans-serif;
                font-size: 1.1rem;
                color: #333;
            }

            .form-group input,
            .form-group textarea {
                width: 100%;
                padding: 0.8rem;
                border: 3px solid #e65d43;
                border-radius: 8px;
                font-size: 1rem;
                background: #fff;
                transition:
                    border-color 0.3s ease,
                    box-shadow 0.3s ease;
            }

            .form-group input:focus,
            .form-group textarea:focus {
                outline: none;
                border-color: #008989;
                box-shadow: 0 0 0 3px rgba(0, 137, 137, 0.1);
            }

            .submit-btn {
                background: linear-gradient(45deg, #e65d43, #e59336);
                color: white;
                border: none;
                padding: 1rem 2rem;
                font-family: "Anton", sans-serif;
                font-size: 1.1rem;
                border-radius: 8px;
                cursor: pointer;
                transition:
                    transform 0.2s ease,
                    box-shadow 0.2s ease;
            }

            .submit-btn:hover {
                transform: translateY(-2px);
                box-shadow: 0 5px 15px rgba(230, 93, 67, 0.4);
            } */
        </style>
    </head>
    <body>
        <main>
            <label for="contact"
                ><div class="red"><span>Contact</span></div></label
            >
            <input type="radio" name="bands" id="contact" />

            <div>
                <section>
                    <h1>Contact</h1>
                    <form class="contact-form">
                        <div class="form-group">
                            <label for="name">Name</label>
                            <input type="text" id="name" name="name" required />
                        </div>

                        <div class="form-group">
                            <label for="email">Email</label>
                            <input
                                type="email"
                                id="email"
                                name="email"
                                required
                            />
                        </div>

                        <div class="form-group">
                            <label for="subject">Subject</label>
                            <input
                                type="text"
                                id="subject"
                                name="subject"
                                required
                            />
                        </div>

                        <div class="form-group">
                            <label for="message">Message</label>
                            <textarea
                                id="message"
                                name="message"
                                rows="6"
                                required></textarea>
                        </div>

                        <button type="submit" class="submit-btn"
                            >Send Message</button
                        >
                    </form>
                </section>
            </div>

            <label for="services"
                ><div class="orange"><span>Services</span></div></label
            >
            <input type="radio" name="bands" id="services" />

            <div>
                <section>
                    <h1>Services</h1>
                    <p>These are our services!</p>
                </section>
            </div>

            <label for="articles"
                ><div class="gold"><span>Articles</span></div></label
            >
            <input type="radio" name="bands" id="articles" />

            <div>
                <section>
                    <h1>Articles</h1>
                    <p>Read as much as you like... it's free!</p>
                </section>
            </div>

            <label for="home"><div class="teal"><span>Home</span></div></label>
            <input type="radio" name="bands" id="home" checked="checked" />
            <div>
                <section>
                    <h1>retrono.me</h1>
                    <p>
                        Welcome to the official <a href="https://astro.build/"
                            >Astro</a
                        >
                        blog starter template. This template serves as a lightweight,
                        minimally-styled starting point for anyone looking to build
                        a personal website, blog, or portfolio with Astro.
                    </p>
                </section>
            </div>
        </main>
    </body>
</html>
